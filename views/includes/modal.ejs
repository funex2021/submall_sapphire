<!-- Modal View Start : Alam Modal -->
<div class="modal fade" id="viewAlarm" tabindex="-1" aria-labelledby="viewAlarmLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="flex items-center px-5 py-2 text-sm bg-gray-200">
                <h5 class="flex items-center" id="viewAlarmLabel">Alert</h5>
            </div>
            <div id="modal-text" class="px-5 py-2 text-xs">
            </div>
            <div class="mb-2 modal-footer">
                <button type="button" class="px-6 py-2 text-xs btn btn-dark" data-bs-dismiss="modal">
                    확인
                </button>
            </div>
        </div>
    </div>
</div>
<!-- Modal View End : Alam Modal -->

<!-- Modal View Start : Alam Modal -->
<div class="modal fade" id="viewConfirmAlarm" tabindex="-1" aria-labelledby="viewConfirmAlarmLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="flex items-center px-5 py-2 text-sm bg-gray-200">
                <h5 class="flex items-center" id="viewConfirmAlarmLabel">Alert</h5>
            </div>
            <div id="viewConfirmText" class="px-5 py-2 text-xs">
            </div>
            <div class="mb-2 modal-footer">
                <button type="button" class="px-6 py-2 text-xs btn btn-dark" id="confirmProc">
                    확인
                </button>
                <button type="button" class="px-6 py-2 text-xs btn btn-dark" data-bs-dismiss="modal">
                    취소
                </button>
            </div>
        </div>
    </div>
</div>
<!-- Modal View End : Alam Modal -->

<!-- Modal View Start : Alam Modal -->
<div class="modal fade" id="viewRefreshAlarm" tabindex="-1" aria-labelledby="viewRefreshAlarmLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="flex items-center px-5 py-2 text-sm bg-gray-200">
                <h5 class="flex items-center" id="viewRefreshAlarmLabel">Alert</h5>
            </div>
            <div id="viewRefreshText" class="px-5 py-2 text-xs">
            </div>
            <div class="mb-2 modal-footer">
                <button type="button" class="px-6 py-2 text-xs btn btn-dark" onclick="location.reload();">
                    확인
                </button>
            </div>
        </div>
    </div>
</div>
<!-- Modal View End : Alam Modal -->

<!-- Modal View Start : Alam Modal -->
<div class="modal fade" id="viewLocationAlarm" tabindex="-1" aria-labelledby="viewLocationAlarmLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="flex items-center px-5 py-2 text-sm bg-gray-200">
                <h5 class="flex items-center" id="viewLocationAlarmLabel">Alert</h5>
            </div>
            <div id="viewLocationText" class="px-5 py-2 text-xs">
            </div>
            <div class="mb-2 modal-footer">
                <button type="button" class="px-6 py-2 text-xs btn btn-dark" id="locationId" onclick="">
                    확인
                </button>
            </div>
        </div>
    </div>
</div>
<!-- Modal View End : Alam Modal -->

<script>
    function fnViewAlarm(content) {
        $("#viewAlarm").modal('show');
        $("#modal-text").html(content);
    }

    function fnViewConfirmAlarm(title, content, callback, data) {
        $("#viewConfirmAlarmLabel").html(title);
        $("#viewConfirmText").html(content)
        $("#confirmProc").attr('onclick', callback);
        $("#viewConfirmAlarm").modal('show');
    }

    function fnViewRefreshAlarm(content) {
        $("#viewRefreshText").html(content);
        $("#viewRefreshAlarm").modal('show');
    }

    function fnViewLocationAlarm(content, loc) {
        $("#viewLocationText").html(content);
        $("#viewLocationAlarm").modal('show');
        $("#locationId").attr('onclick', 'javascript:location.href="'+loc+'"');
    }

    function wrapWindowByMaskHide() {
        $('#mask, #loadingImg').hide();
        $('#mask, #loadingImg').remove();
    }

    function wrapWindowByMaskShow() {
        //화면의 높이와 너비를 구한다.
        var maskHeight = $(document).height();
        //      var maskWidth = $(document).width();
        var maskWidth = window.document.body.clientWidth;

        var mask =
            "<div id='mask' style='position:absolute; z-index:9000; background-color:#000000; display:none; left:0; top:0;'></div>";
        var loadingImg = '';

        loadingImg +=
            "<div id='loadingImg' style='position:absolute; left:50%; top:40%; display:none; z-index:10000;'>";
        loadingImg += " <img src='/img/viewLoading.gif'/>";
        loadingImg += "</div>";

        //화면에 레이어 추가
        $('body')
            .append(mask)
            .append(loadingImg)

        //마스크의 높이와 너비를 화면 것으로 만들어 전체 화면을 채운다.
        $('#mask').css({
            'width': maskWidth,
            'height': maskHeight,
            'opacity': '0.3'
        });

        //마스크 표시
        $('#mask').show();

        //로딩중 이미지 표시
        $('#loadingImg').show();
    }
</script>